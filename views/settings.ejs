<!DOCTYPE html>
<html lang="ru">
<head>
    <%- include('partials/header') %>
</head>
<body class="bg-gray-50 text-manus-text font-inter">
    <div class="max-w-7xl mx-auto px-4 py-8">
        <header class="mb-8 flex justify-between items-end">
            <div>
                <h1 class="text-3xl font-bold mb-2">Настройки</h1>
                <p class="text-gray-500">Управление списком витрин для мониторинга</p>
            </div>
        </header>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Список витрин -->
            <div class="lg:col-span-2">
                <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                    <div class="px-6 py-4 bg-gray-50 border-b border-gray-100 font-semibold">Список активных витрин</div>
                    <ul class="divide-y divide-gray-100">
                        <% showcases.forEach(site => { %>
                            <li class="px-6 py-4 flex justify-between items-center hover:bg-gray-50 transition-colors">
                                <div>
                                    <h3 class="font-bold text-lg"><%= site.name %></h3>
                                    <p class="text-sm text-gray-400 font-mono"><%= site.url %></p>
                                </div>
                                <div class="flex items-center space-x-2">
                                    <button onclick="openSelectorModal('<%= site.id %>', '<%= site.name %>', '<%= site.custom_selector || '' %>')" class="text-gray-400 hover:text-blue-600 p-2 border border-gray-100 rounded-xl hover:bg-blue-50 transition-all" title="Настроить селектор">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                                        </svg>
                                    </button>
                                    <form action="/api/showcases/delete/<%= site.id %>" method="POST" onsubmit="return confirm('Удалить эту витрину и всю статистику по ней?')">
                                        <button type="submit" class="text-red-400 hover:text-red-600 p-2 border border-red-100 rounded-xl hover:bg-red-50 transition-all">
                                            Удалить
                                        </button>
                                    </form>
                                </div>
                            </li>
                        <% }); %>
                    </ul>
                </div>
            </div>

            <!-- Форма добавления -->
            <div class="lg:col-span-1">
                <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6 sticky top-8">
                    <h2 class="text-xl font-bold mb-6">Добавить витрину</h2>
                    <form action="/api/showcases" method="POST" class="space-y-4">
                        <div>
                            <label class="block text-sm font-semibold mb-2 text-gray-600">Название</label>
                            <input type="text" name="name" required placeholder="Например: Robot Zaymer" class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:outline-none focus:ring-2 focus:ring-manus-accent/20 focus:border-manus-accent transition-all">
                        </div>
                        <div>
                            <label class="block text-sm font-semibold mb-2 text-gray-600">URL</label>
                            <input type="url" name="url" required placeholder="https://site.ru/..." class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:outline-none focus:ring-2 focus:ring-manus-accent/20 focus:border-manus-accent transition-all">
                        </div>
                        <button type="submit" class="w-full bg-manus-text text-white font-bold py-4 rounded-xl hover:bg-manus-accent transition-all shadow-lg shadow-gray-200">
                            Сохранить витрину
                        </button>
                    </form>
                    
                    <div class="mt-8 p-4 bg-blue-50 rounded-xl border border-blue-100 italic text-sm text-blue-600 leading-relaxed">
                        После добавления новой витрины рекомендуется запустить ручной парсинг из раздела Дашборд.
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal: Custom Selector -->
    <div id="selectorModal" class="hidden fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full z-50">
        <div class="relative top-20 mx-auto p-5 border w-11/12 md:w-1/2 shadow-lg rounded-md bg-white">
            <div class="flex justify-between items-center pb-3 border-b">
                <div>
                    <h3 class="text-lg font-medium text-gray-900">Настройка парсера</h3>
                    <p class="text-sm text-gray-500 mt-1" id="modalShowcaseName"></p>
                </div>
                <button onclick="closeSelectorModal()" class="text-gray-400 hover:text-gray-600">
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            
            <div class="mt-4">
                <p class="text-sm text-gray-500 mb-4">
                    Вставьте HTML-элемент карточки оффера (outerHTML). Парсер автоматически извлечет селектор из этого элемента.
                    <br><span class="text-xs text-gray-400">Скопируйте элемент из DevTools: ПКМ → Скопировать → Скопировать outerHTML</span>
                </p>
                <input type="hidden" id="modalShowcaseId">
                <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700 mb-1">HTML-элемент карточки</label>
                    <textarea id="modalSelector" rows="8" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500 font-mono text-xs" placeholder="<div class='offer' id='item1'>..."></textarea>
                </div>
            </div>
            
            <div class="flex justify-end space-x-3 mt-5 pt-3 border-t">
                <button onclick="closeSelectorModal()" class="px-4 py-2 bg-gray-200 text-gray-800 rounded-md hover:bg-gray-300 transition-all">
                    Отмена
                </button>
                <button onclick="saveSelector()" class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-all">
                    Сохранить
                </button>
            </div>
        </div>
    </div>

    <script>
        function openSelectorModal(id, name, currentSelector) {
            document.getElementById('modalShowcaseId').value = id;
            document.getElementById('modalShowcaseName').textContent = name;
            document.getElementById('modalSelector').value = currentSelector;
            document.getElementById('selectorModal').classList.remove('hidden');
        }

        function closeSelectorModal() {
            document.getElementById('selectorModal').classList.add('hidden');
        }

        async function saveSelector() {
            const id = document.getElementById('modalShowcaseId').value;
            const htmlElement = document.getElementById('modalSelector').value.trim();
            
            if (!htmlElement) {
                alert('Вставьте HTML-элемент');
                return;
            }
            
            try {
                const res = await fetch(`/api/showcase/${id}/update`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ custom_selector: htmlElement })
                });
                
                if (res.ok) {
                    alert('Настройки сохранены');
                    window.location.reload();
                } else {
                    alert('Ошибка сохранения');
                }
            } catch (e) {
                console.error(e);
                alert('Ошибка сети');
            }
        }
    </script>
</body>
</html>
