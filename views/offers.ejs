<!DOCTYPE html>
<html lang="ru">
<head>
    <%- include('partials/header') %>
</head>
<body class="bg-gray-50 text-manus-text font-inter">
    <div class="max-w-7xl mx-auto px-4 py-8">
        <header class="mb-8">
            <h1 class="text-3xl font-bold mb-2">Все офферы</h1>
            <p class="text-gray-500">Сводная аналитика по всем компаниям во всех витринах</p>
        </header>

        <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
            <table class="w-full text-left">
                <thead class="bg-gray-50 border-b border-gray-100">
                    <tr>
                        <th class="px-6 py-4 font-semibold text-sm">Компания</th>
                        <th class="px-6 py-4 font-semibold text-sm">Средняя позиция</th>
                        <th class="px-6 py-4 font-semibold text-sm">Охват витрин</th>
                        <th class="px-6 py-4 font-semibold text-sm">Частота (%)</th>
                        <th class="px-6 py-4 font-semibold text-sm">Статус</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-100">
                    <% if (offers.length === 0) { %>
                        <tr>
                            <td colspan="5" class="px-6 py-12 text-center text-gray-400">
                                Данных пока нет. Запустите первый парсинг.
                            </td>
                        </tr>
                    <% } %>
                    <% offers.forEach(offer => { %>
                        <tr class="hover:bg-gray-50 transition-colors">
                            <td class="px-6 py-4">
                                <div class="flex items-center space-x-3">
                                    <div class="w-10 h-10 rounded-lg bg-gray-50 flex items-center justify-center p-1 border border-gray-100 overflow-hidden">
                                        <% if (offer.logo) { %>
                                            <img src="<%= offer.logo %>" class="max-w-full max-h-full object-contain" alt="<%= offer.company_name %>">
                                        <% } else { %>
                                            <div class="text-[10px] text-gray-400 font-bold uppercase"><%= offer.company_name.substring(0,2) %></div>
                                        <% } %>
                                    </div>
                                    <span class="font-medium text-gray-900"><%= offer.company_name %></span>
                                </div>
                            </td>
                            <td class="px-6 py-4">
                                <span class="px-2.5 py-1 bg-manus-accent/10 text-manus-accent rounded-md text-sm font-bold">
                                    <%= offer.avg_pos.toFixed(2) %>
                                </span>
                            </td>
                            <td class="px-6 py-4 text-sm font-medium text-gray-700">
                                <%= offer.showcase_count %> из <%= offer.total_showcases %>
                            </td>
                            <td class="px-6 py-4">
                                <div class="flex items-center">
                                    <div class="w-16 bg-gray-100 rounded-full h-1.5 mr-2">
                                        <div class="bg-blue-500 h-1.5 rounded-full" style="width: <%= offer.frequency_pct %>%;"></div>
                                    </div>
                                    <span class="text-xs font-semibold text-gray-500"><%= offer.frequency_pct %>%</span>
                                </div>
                            </td>
                            <td class="px-6 py-4">
                                <span class="flex items-center text-xs font-semibold text-green-500">
                                    <span class="w-1.5 h-1.5 bg-green-500 rounded-full mr-2"></span>
                                    Active
                                </span>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
